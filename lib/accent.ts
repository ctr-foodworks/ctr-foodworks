import type { VendorAccent } from "./types";

export const accentVar: Record<VendorAccent, string> = {
  primary: "var(--primary)",
  ochre: "var(--secondary-ochre)",
  navy: "var(--secondary-navy)",
  plum: "var(--secondary-plum)",
};
